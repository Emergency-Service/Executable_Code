{{>layouts/header}}
<style>
    main {
            width: 70%;
            background: #ffffff;
            margin: 30px auto;
            padding: 30px;
            border: 1px solid #ccc;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        }
        h2 {
            font-size: 28px;
            margin-bottom: 20px;
        }
        .form-row {
            display: flex;
            justify-content: space-between;
            gap: 20px;
            margin-bottom: 25px;
        }
        .form-group {
            flex: 1;
            display: flex;
            flex-direction: column;
        }
        .form-group input {
            padding: 15px;
            font-size: 20px;
            border: 1px solid #ccc;
            border-radius: 8px;
            margin-top: 10px;
            margin-bottom: 10px;
        }
        button {
            background-color: #0056b3;
            color: #ffffff;
            font-size: 22px;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            display: block;
            margin: 0 auto;
        }
        button:hover {
            background-color: #003d80;
        }
        table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 30px;
        }
        table th, table td {
            border: 1px solid #ccc;
            padding: 10px;
            text-align: center;
        }
        table th {
            background-color: #f9f9f9;
            font-weight: bold;
        }
        table td {
            background-color: #ffffff;
        }
</style>
<main>
    <h2>응급 상황 설명</h2>
    <div class="form-group">
        <label for="summary">상황 메시지 요약</label>
<!--        <input type="text" id="summary" value="{summary}" readonly>-->
    </div>
    <div class="form-row">
        <div class="form-group">
            <label for="keywords">키워드</label>
<!--            <input type="text" id="keywords" value="{keywords}" readonly>-->
        </div>
        <div class="form-group">
            <label for="predicted-class">예측 클래스</label>
            <input type="text" id="predicted-class" value="{{model}}" readonly>
        </div>
    </div>
    <form action="/re_input" method="GET">
        <button>다시하기</button>
    </form>
    <h3>응급실 추천순</h3>
    <table>
        <thead>
        <tr>
            <th>번호</th>
            <th>병원 이름</th>
            <th>전화번호</th>
            <th>주소</th>
            <th>거리 (km)</th>
            <th>소요시간</th>
            <th>도착예정시각</th>
        </tr>
        </thead>
        <tbody>
<!--        {#nearest_hospitals}-->
<!--            <tr>-->
<!--                <td>{@index}</td>-->
<!--                <td>{hospital_name}</td>-->
<!--                <td>{tel1}</td>-->
<!--                <td>{}</td>-->
<!--                <td>{거리 (km)}</td>-->
<!--                <td>{소요시간}</td>-->
<!--                <td>{도착예정시각}</td>-->
<!--            </tr>-->
<!--        {/nearest_hospitals}-->
<!--        {^nearest_hospitals}-->
<!--            <tr>-->
<!--                <td colspan="7">데이터 없음</td>-->
<!--            </tr>-->
<!--        {/nearest_hospitals}-->
        </tbody>
    </table>
</main>
{{>layouts/footer}}