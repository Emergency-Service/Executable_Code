<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>응급 상황 설명</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            align-items: center;
            background-color: #f4f4f4;
            font-size: 18px; /* 기본 글자 크기 추가 확대 */
            height: 100%;
        }
        header {
            width: 100%;
            background-color: #ffffff;
            padding: 30px 20px; /* 헤더 패딩 확대 */
            border-bottom: 1px solid #ccc;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
            margin-right: 40px;
        }
        header img {
            height: 70px; /* 이미지 크기 추가 확대 */
        }
        .header-buttons {
            position: absolute;
            top: 60px;
            right: 20px;
            display: flex;
            gap: 20px; /* 버튼 간격 확대 */
            align-items: center;
        }
        .header-buttons button {
            background-color: transparent;
            color: #333;
            font-size: 20px; /* 버튼 글자 크기 추가 확대 */
            padding: 15px 25px; /* 버튼 패딩 확대 */
            border: 1px solid #ccc;
            border-radius: 25px;
            cursor: pointer;
            transition: all 0.3s ease;
        }
        .header-buttons button:hover {
            background-color: #f0f0f0;
            color: #000;
        }
        .search-bar {
            display: flex;
            align-items: center;
            border: 1px solid #ccc;
            border-radius: 25px;
            padding: 10px 20px; /* 검색바 패딩 확대 */
            background-color: #fff;
            gap: 15px; /* 검색창 내부 간격 확대 */
        }
        .search-bar input {
            border: none;
            outline: none;
            width: 240px; /* 검색창 너비 확대 */
            font-size: 20px; /* 검색창 글자 크기 추가 확대 */
        }
        .search-bar button {
            background-color: transparent;
            border: none;
            cursor: pointer;
            display: flex;
            align-items: center;
        }
        .search-bar button img {
            width: 24px;
            height: 24px; /* 아이콘 크기 확대 */
        }
        .header-left img {
            position: absolute;
            top: 15px;
            left: 15px;
            width: 350px; /* 이미지 크기 추가 확대 */
            height: auto;
        }
        .header-center img {
            width: 350px; /* 이미지 크기 추가 확대 */
            height: auto;
            display: block;
            margin: 0 auto;
        }
        main {
            width: 70%; /* 메인 컨텐츠 너비 확대 */
            background: #ffffff;
            margin: 30px auto;
            padding: 30px;
            border: 1px solid #ccc;
            border-radius: 10px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.2);
        }
        h2 {
            font-size: 28px; /* 제목 글자 크기 추가 확대 */
            margin-bottom: 20px;
        }
        .form-group {
            margin-bottom: 25px;
        }
        label {
            display: block;
            font-weight: bold;
            margin-bottom: 10px;
            font-size: 22px; /* 라벨 글자 크기 추가 확대 */
        }
        textarea, input {
            width: 100%;
            padding: 15px; /* 입력 필드 패딩 확대 */
            font-size: 20px; /* 입력 필드 글자 크기 추가 확대 */
            border: 1px solid #ccc;
            border-radius: 8px;
            box-sizing: border-box;
        }
        input{
            width: 104%;
        }
        textarea {
            height: 180px; /* 텍스트 영역 높이 추가 확대 */
            resize: none;
        }
        .input-group {
            display: flex;
            gap: 20px; /* 입력 필드 간격 확대 */
        }
        .example {
            font-size: 18px; /* 예시 텍스트 크기 추가 확대 */
            color: #888;
            margin-top: 8px;
        }
        button {
            background-color: #0056b3;
            color: #ffffff;
            font-size: 22px; /* 버튼 글자 크기 추가 확대 */
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
        }
        button:hover {
            background-color: #003d80;
        }
        footer {
            width: 100%; 
            background-color: #ffffff; 
            padding: 60px 0; 
            border-top: 1px solid #ccc;
            text-align: center; 
            font-size: 18px; 
            color: #555
        }
        .foot_div1{
            margin-bottom: 10px;
        }
        .foot_div2{
            display: flex; 
            justify-content: center; 
            gap: 20px; 
            flex-wrap: wrap;
        }
        .foot_div2 a{
            color: #0056b3; 
            text-decoration: none; 
            transition: color 0.3s;
        }
        .modal {
            display: none; /* 기본적으로 숨김 */
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background-color: rgba(0, 0, 0, 0.5);
            z-index: 1000;
            justify-content: center;
            align-items: center;
        }

        /* 모달 콘텐츠 스타일 */
        .modal-content {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 5px;
            width: 80%;
            max-width: 500px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
            text-align: left;
            max-height: 80vh;
            overflow-y: auto;
        }

        /* 닫기 버튼 스타일 */
        .close-button {
            float: right;
            font-size: 1.5rem;
            font-weight: bold;
            cursor: pointer;
            color: #333;
        }
        .hamburger-button {
    position: absolute;
    top: 15px;
    right: 15px;
    font-size: 24px;
    background: none;
    border: none;
    cursor: pointer;
    z-index: 1001;
    display: none; /* 기본적으로 숨김, 모바일에서만 보이게 설정 */
}
        .mobile-menu {
    position: fixed;
    top: 0;
    right: -100%;
    width: 300px;
    height: 100%;
    background-color: #ffffff;
    box-shadow: 0 0 15px rgba(0, 0, 0, 0.3);
    display: flex;
    flex-direction: column;
    justify-content: flex-start;
    align-items: flex-start;
    padding: 20px;
    transition: right 0.3s ease;
    z-index: 1000;
}

.mobile-menu.open {
    right: 0;
}

.mobile-menu-content button {
    background: none;
    color: #333;
    font-size: 18px;
    padding: 10px 20px;
    border: 1px solid #ccc;
    border-radius: 8px;
    margin-bottom: 10px;
    cursor: pointer;
    width: 100%;
}

.mobile-menu-content .search-bar {
    display: flex;
    align-items: center;
    width: 100%;
    border: 1px solid #ccc;
    border-radius: 25px;
    padding: 10px 15px;
    margin-bottom: 20px;
    background-color: #fff;
}

.mobile-menu-content .search-bar input {
    border: none;
    outline: none;
    width: 100%;
    font-size: 16px;
}

.mobile-menu-content .search-bar button img {
    width: 20px;
    height: 20px;
}

.close-menu {
    background: none;
    border: none;
    font-size: 24px;
    cursor: pointer;
    align-self: flex-end;
    margin-bottom: 20px;
}
        @media (max-width: 1200px) {
    body {
        font-size: 16px; /* 기본 글자 크기 축소 */
    }
    header img {
        height: 60px; /* 헤더 이미지 크기 축소 */
    }
    .header-buttons {
        top: 40px;
        gap: 15px; /* 버튼 간격 축소 */
    }
    .search-bar input {
        width: 200px; /* 검색창 너비 축소 */
        font-size: 18px;
    }
    .header-center img {
        width: 300px; /* 중앙 이미지 크기 축소 */
    }
    main {
        width: 80%; /* 메인 너비 축소 */
        padding: 20px; /* 패딩 축소 */
    }
    button {
        font-size: 20px;
        padding: 12px 25px;
    }
    textarea, input {
        font-size: 18px;
        padding: 10px; /* 입력 필드 패딩 축소 */
    }
    .input-group {
        gap: 15px; /* 입력 필드 간격 축소 */
    }
    table th, table td {
        padding: 8px; /* 테이블 셀 패딩 축소 */
    }
}

@media (max-width: 480px) {
    .form-row {
        flex-direction: column; /* 세로 배치 */
        gap: 10px; /* 요소 간 간격 조정 */
    }
    header {
        padding: 15px 10px;
        flex-direction: column;
        align-items: flex-start;
        display: none;
    }
    .hamburger-button {
        display: block;
        position: fixed;
        top: 10px;
        right: 10px;
        z-index: 1000;
        border: none;
        cursor: pointer; /* 커서 모양 변경 */ 
    }
    .hamburger-icon {
    width: 30px; /* 버튼 크기 */
    height: auto; /* 비율 유지 */
}
    .header-left img {
        width: 200px; /* 로고 크기 축소 */
    }
    .header-buttons {
        top: auto;
        right: auto;
        flex-direction: column; /* 버튼 세로 정렬 */
        gap: 10px;
    }
    .search-bar {
        width: 100%; /* 검색창 너비를 100%로 조정 */
        padding: 10px;
        gap: 10px;
    }
    .search-bar input {
        width: 100%; /* 입력 필드를 부모 너비에 맞춤 */
        font-size: 14px;
    }
    .header-center img {
        width: 150px; /* 이미지 크기 축소 */
        margin-top: 10px;
    }

    main {
        width: 95%; /* 메인 컨텐츠 너비 조정 */
        padding: 15px;
    }
    h2 {
        font-size: 20px; /* 제목 크기 축소 */
    }
    .form-group label {
        font-size: 16px; /* 라벨 크기 축소 */
    }
    .form-group input {
        font-size: 14px; /* 입력 필드 글자 크기 축소 */
        padding: 10px;
        width: 80%;
    }
    button {
        font-size: 16px; /* 버튼 크기 축소 */
        padding: 10px 20px;
    }
    table {
        font-size: 12px; /* 테이블 글자 크기 축소 */
    }
    table th, table td {
        padding: 8px; /* 셀 패딩 축소 */
    }
    table {
        overflow-x: auto; /* 테이블 스크롤 추가 */
        display: block;
    }
    .modal-content {
        width: 90%; /* 모달 너비 축소 */
        padding: 15px;
    }
}

/* 768px 이하 (태블릿 화면) */
@media (max-width: 768px) {
    .form-row {
        flex-direction: column; /* 세로 배치 */
        gap: 10px; /* 요소 간 간격 조정 */
    }
    header {
        padding: 20px 10px;
        display: none;
    }
    .hamburger-button {
        display: block;
        position: fixed;
        top: 10px;
        right: 10px;
        z-index: 1000;
        border: none;
        cursor: pointer; /* 커서 모양 변경 */ 
    }
    .hamburger-icon {
        width: 30px;
        height: auto;
        position: fixed;
        top: 50px;
        right: 30px;
    }
    .header-left img {
        width: 250px;
    }
    .header-center img {
        width: 200px;
    }
    .search-bar input {
        width: 100%;
        font-size: 16px;
    }
    main {
        width: 90%;
    }
    h2 {
        font-size: 22px;
    }
    .form-group label {
        font-size: 18px;
    }
    .form-group input {
        font-size: 14px; /* 입력 필드 글자 크기 축소 */
        padding: 10px;
        width: 80%;
    }
    button {
        font-size: 18px;
        padding: 12px 25px;
    }
    table th, table td {
        font-size: 14px;
        padding: 10px;
    }
    .modal-content {
        width: 80%;
        padding: 20px;
    }
}

    </style>
</head>
<body>
    <button class="hamburger-button" aria-label="메뉴 열기">
        <img src="ham.png" class="hamburger-icon">
    </button>
    <div class="mobile-menu">
        <button class="close-menu" aria-label="메뉴 닫기">&times;</button>
        <div class="mobile-menu-content">
            <div class="search-bar">
                <input type="text" placeholder="검색어를 입력하세요">
            </div>
            <button onclick="location.href='mypage.html'">마이 페이지</button>
            <button onclick="location.href='login3.html'">로그아웃</button>
        </div>
    </div>

    <header>
        <div class="header-left">
            <img src="img4.png" alt="Slogan Image">
        </div>
        <div class="header-center">
            <img src="img3.png" alt="KakaoTalk Image">
        </div>
        <div class="header-buttons">
            <div class="search-bar">
                <input type="text" placeholder="검색어를 입력하세요">
                <button>
                    <img src="img5.png" alt="검색 아이콘">
                </button>
            </div>
            <button onclick="location.href='mypage.html'">마이 페이지</button>
            <button onclick="location.href='login3.html'">로그아웃</button>
        </div>
    </header>
    <main>
        <meta charset="UTF-8">
        <h2>응급 상황 설명</h2>
        <form action="/hospital/recommend_hospital" method="Post">
            <div class="form-group">
                <label for="text">상황 메시지 (필수)</label>
                <textarea id="text" name="text" placeholder="상황을 최대한 자세히 설명해주세요."></textarea>
            </div>
            <div class="form-group input-group">
                <div>
                    <label for="latitude">위도 (선택)</label>
                    <input type="text" id="latitude" name="latitude" placeholder="위도를 입력해주세요.">
                </div>
                <div>
                    <label for="longitude">경도 (선택)</label>
                    <input type="text" id="longitude" name="longitude" placeholder="경도를 입력해주세요.">
                </div>
                <div>
                    <label for="count">추천 응급실 수 (선택)</label>
                    <input type="text" id="count" name="count" placeholder="응급실 개수를 입력해주세요.">
                </div>
            </div>
            <p class="example">예시: 서울시청 - 위도: 37.571513, 경도: 126.734086</p>
            <button type="submit">검색</button>
        </form>
    </main>
    <footer>
        <div class="foot_div1">
            © 2024 Emergency Assist Platform. All rights reserved.
        </div>
        <div class="foot_div2">
            <a href="#" class="modal-trigger" data-modal="privacy-modal">개인정보처리방침</a>
            <a href="#" class="modal-trigger" data-modal="terms-modal">이용약관</a>
            <a href="#" class="modal-trigger" data-modal="support-modal">고객센터</a>
            <a href="#" class="modal-trigger" data-modal="inquire-modal">문의하기</a>
        </div>

        <!-- 모달 팝업들 -->
        <div id="privacy-modal" class="modal">
            <div class="modal-content">
                <span class="close-button">&times;</span>
                <h2>개인정보 처리방침</h2>
                <p>■ 보건복지부는 정보주체의 자유와 권리 보호를 위해 「개인정보 보호법」 및 관계 법령이 정한 바를 준수하여, 적법하게 개인정보를 처리하고 안전하게 관리하고 있습니다.</p>
                <p>■ 이에 「개인정보 보호법」 제30조에 따라 정보주체에게 개인정보 처리에 관한 절차 및 기준을 안내하고, 이와 관련한 고충을 신속하고 원활하게 처리할 수 있도록 하기 위하여 다음과 같이 개인정보 처리방침을 수립·공개합니다.</p>
            </div>
        </div>

        <div id="terms-modal" class="modal">
                <div class="modal-content">
                    <span class="close-button">&times;</span>
                    <h2>이용 약관</h2>
                    <h4>제1조 (목적)</h4>
                    <p>
                    이 약관은 보건 27팀의 응급 연계 서비스를 이용함에 있어 이용자와 운영기관인 KT AIVLE SCHOOL 간의 권리·의무 및 책임사항과 기타 필요한 사항을 규정함을 목적으로 합니다.
                    </p>
                    <h4>제2조 (약관의 효력과 변경)</h4>
                    <p>
                        ① 이 약관은 서비스를 이용하고자 하는 모든 이용자에 대하여 그 효력을 발생합니다.<br>
                        ② 이 약관의 내용은 서비스의 일부 화면 또는 기타 방법 등에 의하여 이를 공지하거나 그 내용을 이용자에게 통지함으로써 효력이 발생됩니다.<br>
                        ③ 의정원은 필요하다고 인정되는 경우 이 약관의 내용을 변경할 수 있으며, 변경된 약관은 제2항과 같은 방법으로 공지 또는 통지하여 이용자가 직접 확인할 수 있도록 할 것입니다.<br>
                        ④ 이용자는 변경된 약관에 동의하지 않을 경우 서비스 이용을 중단하고 본인의 회원등록을 취소할 수 있으며, 계속 사용하시는 경우에는 약관 변경에 동의한 것으로 간주되어 변경된 약관은 전항과 같은 방법으로 효력이 발생합니다.<br>
                        ⑤ 이용자가 이 약관의 내용에 동의하는 경우 의정원의 서비스 제공행위 및 이용자의 서비스 이용행위에는 이 약관이 우선적으로 적용될 것입니다. 이 약관에 명시되지 않은 사항에 대해서는 개인정보보호법, 전기통신기본법, 전기통신사업법, 정보통신윤리위원회심의규정, 정보통신윤리강령, 정보통신망이용촉진 및 정보보호 등에 관한 법률, 전자서명법 등 기타 대한민국의 관련법령, 의정원이 별도로 정한 규정에 의합니다.
                    </p>
                    <h4>제3조 (계약사항의 변경)</h4>
                    <p>
                        회원은 회원정보관리를 통해 언제든지 자신의 정보를 열람하고 수정할 수 있습니다. 회원은 이용신청 시 기재한 사항이 변경되었을 때에는 수정을 하여야 하며, 수정하지 아니하여 발생하는 문제의 책임은 회원에게 있습니다.
                    </p>
                </div>
            </div>

        <div id="support-modal" class="modal">
                <div class="modal-content">
                    <span class="close-button">&times;</span>
                    <h2>고객 센터</h2>
                    <p>010-xxxx-xxxx</p>
                </div>
            </div>

        <div id="inquire-modal" class="modal">
            <div class="modal-content">
                <span class="close-button">&times;</span>
                <p>무엇을 도와드릴까요?</p>
            </div>
        </div>
    </footer>

    <script>
        // 모든 모달 트리거와 모달 닫기 버튼 처리
            document.addEventListener('DOMContentLoaded', function () {
            // 모달 트리거 요소 가져오기
            const triggers = document.querySelectorAll('.modal-trigger');
            const closeButtons = document.querySelectorAll('.close-button');
            const modals = document.querySelectorAll('.modal');

            // 모달 열기
            triggers.forEach(trigger => {
                trigger.addEventListener('click', function (e) {
                    e.preventDefault(); // 기본 링크 동작 방지
                    const modalId = this.dataset.modal; // data-modal 속성 값 가져오기
                    const modal = document.getElementById(modalId);
            if (modal) {
                modal.style.display = 'flex';
            }
        });
    });

    // 모달 닫기
    closeButtons.forEach(button => {
        button.addEventListener('click', function () {
            const modal = this.closest('.modal');
            modal.style.display = 'none';
        });
    });

    // 모달 외부 클릭 시 닫기
    window.addEventListener('click', function (e) {
        modals.forEach(modal => {
            if (e.target === modal) {
                modal.style.display = 'none';
            }
        });
    });
});
function openMyPage() {
    const panel = document.getElementById('myPagePanel');
    panel.classList.add('open'); // 'open' 클래스 추가
}

// 패널 닫기
function closeMyPage() {
    const panel = document.getElementById('myPagePanel');
    panel.classList.remove('open'); // 'open' 클래스 제거
}
document.addEventListener('DOMContentLoaded', function () {
    const hamburgerButton = document.querySelector('.hamburger-button');
    const mobileMenu = document.querySelector('.mobile-menu');
    const closeMenuButton = document.querySelector('.close-menu');

    // 햄버거 버튼 클릭 시 메뉴 열기
    hamburgerButton.addEventListener('click', () => {
        mobileMenu.classList.add('open');
    });

    // 닫기 버튼 클릭 시 메뉴 닫기
    closeMenuButton.addEventListener('click', () => {
        mobileMenu.classList.remove('open');
    });

    // 메뉴 외부 클릭 시 닫기
    window.addEventListener('click', (e) => {
        if (e.target === mobileMenu) {
            mobileMenu.classList.remove('open');
        }
    });
});
    </script>
</body>
</html>